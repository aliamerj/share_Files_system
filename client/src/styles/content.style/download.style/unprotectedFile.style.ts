import { styled } from "@mui/material/styles";

export const ContainerStyle = styled("div")(() => ({
  display: "flex",
  flexDirection: "column",
}));
export const TitleStyle = styled("h1")(() => ({
  display: "flex",
}));
export const FileNameStyle = styled("h3")(() => ({
  display: "flex",
  color: "#677689",
  alignSelf: "center",
}));
